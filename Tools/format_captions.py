# scrape auto_gen_captions timestamps, insert into our prewritten captions

import json
import sys

def format_captions(original_captions: str, auto_captions: str) -> None:
    None

if __name__ == "__main__":
    original_path, auto_path = sys.argv[1], sys.argv[2]
    format_captions(original_path, auto_path)
    